# dailysync-gramin-docker 环境变量配置
# 复制本文件为 .env 并修改其中的值

# MariaDB root 密码
MYSQL_ROOT_PASSWORD=change_this_root_password

# dailysync 应用数据库用户密码
DB_PASS=change_this_db_password

# Garmin 密码加密密钥（修改后已加密数据会失效，需重新保存配置）
ENC_KEY=change_this_enc_key
